#!/usr/bin/env bash

GS_VERSION='10.03.1'

echo "-----> Check Ghostscript version"

gs --version | grep $GS_VERSION && true
if (( $? != 0 )) ; then
    echo "Invalid Ghostscript version: $(gs --version)"
    echo "Expected version: $(echo $GS_VERSION)"
    exit 1
fi
